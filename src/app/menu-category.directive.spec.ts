import { MenuCategoryDirective } from './menu-category.directive';

describe('MenuCategoryDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuCategoryDirective();
    expect(directive).toBeTruthy();
  });
});
